#include <iostream>
#include <string>
#include <stdlib.h>
#include <iomanip> 

class Time {

	private:
	int hours;
	int minutes;
	int seconds;

	public:
	Time();
	Time(int h, int m, int s);
	~Time();
	 int getHour();
	 int getMinute();
	 int getSecond();
	 void setHours(int h);
	 void setMinutes(int m);
	 void setSeconds(int s);



};


	
	
